{
  "name": "my-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-domain business management platform",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "dev": "turbo dev --parallel",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "clean": "turbo clean && rm -rf node_modules pnpm-lock.yaml",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "seed": "pnpm --filter @platform/db seed",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "husky": "^9.0.0",
    "prettier": "^3.2.0",
    "supabase": "^2.105.0",
    "turbo": "^2.0.0",
    "typescript": "^5.3.0"
  }
}
