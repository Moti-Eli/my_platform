# Dependencies
node_modules/
# pnpm-lock.yaml is COMMITTED on purpose — it makes installs reproducible and CI a
# real gate (works-in-CI == works-for-a-teammate). Only the non-pnpm lockfiles are
# ignored, to avoid accidentally committing a competing lockfile.
yarn.lock
package-lock.json

# Environment
.env
.env.local
.env.*.local

# Build outputs
dist/
build/
.next/
.expo/
out/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Turbo
.turbo/

# OS
Thumbs.db
