// Committed counterpart to Expo's auto-generated `expo-env.d.ts` (which is
// gitignored and only created when the dev server runs). Keeping this reference
// in source means `tsc --noEmit` resolves Expo's ambient types — including the
// typed `process.env.EXPO_PUBLIC_*` variables — in CI without first running Expo.
/// <reference types="expo/types" />
